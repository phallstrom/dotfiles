- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/Library/Fonts/Monaco\ for\ Powerline.otf: fonts/Monaco\ for\ Powerline.otf
    ~/.bash_completion: bash/bash_completion
    ~/.bash_profile: bash/bash_profile
    ~/.bashrc: bash/bashrc
    ~/.vim: vim
    ~/.vimrc: vim/vimrc

- shell: []
